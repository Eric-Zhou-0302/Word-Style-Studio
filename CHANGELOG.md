# 更新记录

仅记录已经实现的功能与修复。`Unreleased` 为尚未发布的工作区内容，不表示已创建 GitHub Release 或已部署网站。

## Unreleased

暂无未发布变更。

## v0.1.0 — 2026-09-25

### 功能

- 浏览器端编辑段落、字符、链接及表格样式，支持继承、后续段落、快速样式和格式属性。
- 编辑项目符号、单级编号和九级多级列表，支持动态编号、重启规则、图片项目符号与样式双向绑定。
- 导出原生 OOXML 空白 DOCX、DOTX 模板和验收样例，支持 JSON 备份与恢复、DOCX/DOTX 样式提取导入及损失报告。
- 提供即时预览、报告场景预览、撤销重做、浏览器本地保存和独立使用说明页面。
- 内置 29 个独立的报告专用样式，示例方案不再包含正文基础样式 Normal，包含页面设置、三套默认列表和标准表格。

- 增加产品首页、真实工作区预览、作者与 GitHub 入口，并保留工作区与文档之间的编辑状态。
- 编号字体按属性提供跟随、单独设置及恢复跟随；颜色选择支持常用色弹层和更多颜色。
- 左侧各组超过三项时支持展开/收起，搜索时显示全部匹配结果。

### 修复与改进

- 工作区隐藏时暂停撤销快捷键并移除模态遮罩，避免首页和文档页误改方案或被对话框阻挡。
- 删除不存在的样式时保持原方案，避免单样式方案被意外替换。

- 格式字段区分“文档默认”和“继承”，同步布尔选项、边框/制表位及重置提示，并标注基础样式来源。

- 方案管理提供“从示例新建”和“从空白新建”；后者仅保留 Normal，重置基础格式及页面设置。

- 修复外部 Word 导入未读取标准自动更新样式标记的问题，支持显式开启、关闭和缺省值。
- 表格预览合并基础样式的条件区域，显示区域边框、行列条带大小，并提供仅影响预览的区域开关。
- 编号测试扩展至全部九级，新增当前级连续编号、上级变化和自定义层级序列。

### 兼容性与校验

- 区分段落/字符样式的 toggle 语义与表格样式的绝对布尔值，保留显式关闭格式的意图。
- 默认第九级重启规则省略 `lvlRestart`，避免 Word 对显式值 `8` 的兼容问题。
- 校验样式继承、标识符、双向编号引用和导入文件；覆盖 ZIP 路径、重复记录、加密标记、解压大小、CRC 与 DTD 风险。

### 发布与许可

- 增加 Node.js 24 版本提示、GitHub CI 和私密安全问题反馈入口；仅维护最新正式发布版本。
- 增加 Vercel 静态部署配置，构建前执行测试。
- 采用 PolyForm Noncommercial 1.0.0 非商业源码许可，并随站点分发项目许可与第三方声明。
- 核对使用文档全部 13 章，修正操作说明并明确导入、预览和撤销边界。

### 已知边界

- 网页预览不模拟完整 Word 排版；外部文档导入不保留正文、宏和所有未知属性，不承诺无损往返。
- 完整 Microsoft Word 保存重开、列表库及跨客户端验收仍未完成，使用边界见 [README.md](README.md)。
